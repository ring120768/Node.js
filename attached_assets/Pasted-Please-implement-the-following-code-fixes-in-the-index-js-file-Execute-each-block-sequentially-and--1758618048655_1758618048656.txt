Please implement the following code fixes in the index.js file. Execute each block sequentially and confirm completion before moving to the next.
Block 1: Fix Critical Bug in Signup Webhook
Location: Find the /webhook/signup endpoint (around line 3394)
Current problematic code:
javascriptapp.post('/webhook/signup', checkSharedKey, async (req, res) => {
  try {
    Logger.info('Signup webhook received');
    Logger.debug('Webhook payload:', JSON.stringify(req.body, null, 2));

    // Debug with enhanced debugger
    if (webhookDebugger) {
      const analysis = webhookDebugger.analyzeWebhook(req, { store: true });
      Logger.info('Webhook analysis:', {
        provider: analysis.provider,
        userId: analysis.fields.userId,
        hasConsent: !!analysis.fields.consent
      });
    }

    // ENHANCED CONSENT DETECTION
    let consentResult = { hasConsent: false };
    
    if (consentManager) {
      // Use enhanced consent detection
      const consentData = consentManager.extractConsentFromWebhook(webhookData);
Replace with:
javascriptapp.post('/webhook/signup', checkSharedKey, async (req, res) => {
  try {
    Logger.info('Signup webhook received');
    const webhookData = req.body;  // MOVED HERE - FIX: Define webhookData before using it
    Logger.debug('Webhook payload:', JSON.stringify(webhookData, null, 2));

    // Debug with enhanced debugger
    if (webhookDebugger) {
      const analysis = webhookDebugger.analyzeWebhook(req, { store: true });
      Logger.info('Webhook analysis:', {
        provider: analysis.provider,
        userId: analysis.fields.userId,
        hasConsent: !!analysis.fields.consent
      });
    }

    // ENHANCED CONSENT DETECTION
    let consentResult = { hasConsent: false };
    
    if (consentManager) {
      // Use enhanced consent detection
      const consentData = consentManager.extractConsentFromWebhook(webhookData);
Also remove the duplicate line: Later in the same function (around line 3437), remove this duplicate line:
javascriptconst webhookData = req.body;  // DELETE THIS LINE - it's now at the top

Block 2: Add Missing Consent Management Endpoints
Location: After the GDPR routes section (around line 2577, after the GDPR routes registration)
Add this new code block:
javascript// ========================================
// ENHANCED CONSENT MANAGEMENT ENDPOINTS
// ========================================

// Get consent summary for a user
app.get('/api/consent/summary/:userId', checkSharedKey, async (req, res) => {
  if (!consentManager) {
    return res.status(503).json({ 
      error: 'Consent manager not initialized',
      requestId: req.requestId 
    });
  }
  
  try {
    const summary = await consentManager.getConsentSummary(req.params.userId);
    
    res.json({
      success: true,
      ...summary,
      requestId: req.requestId
    });
  } catch (error) {
    Logger.error('Error getting consent summary:', error);
    res.status(500).json({
      error: 'Failed to get consent summary',
      details: error.message,
      requestId: req.requestId
    });
  }
});

// Test consent extraction
app.post('/api/consent/test-extraction', checkSharedKey, async (req, res) => {
  if (!consentManager) {
    return res.status(503).json({ 
      error: 'Consent manager not initialized',
      requestId: req.requestId 
    });
  }
  
  try {
    const consentData = consentManager.extractConsentFromWebhook(req.body);
    
    res.json({
      success: true,
      extraction: consentData,
      requestId: req.requestId
    });
  } catch (error) {
    Logger.error('Error testing consent extraction:', error);
    res.status(500).json({
      error: 'Failed to test consent extraction',
      details: error.message,
      requestId: req.requestId
    });
  }
});

Logger.info('✅ Consent management endpoints registered');

Block 3: Update Home Page HTML (Optional Enhancement)
Location: In the root endpoint / (around line 2491)
Find this section in the HTML:
javascript            <div class="endpoint">
                <strong>Core Services:</strong><br>
Add these new endpoints to the list:
javascript            <div class="endpoint">
                <strong>Core Services:</strong><br>
                <code>GET /health</code> - System health check with service status<br>
                <code>GET /api/config</code> - Get Supabase configuration<br>
                <code>GET /api/consent/summary/:userId</code> - Get consent summary for user <span class="new-badge">NEW</span><br>
                <code>POST /api/consent/test-extraction</code> - Test consent field extraction <span class="new-badge">NEW</span><br>
                <code>GET /api/debug/user/:userId</code> - Debug user data with consent status<br>