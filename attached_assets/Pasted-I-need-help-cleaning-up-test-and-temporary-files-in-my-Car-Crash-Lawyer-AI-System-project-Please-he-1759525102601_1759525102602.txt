I need help cleaning up test and temporary files in my Car Crash Lawyer AI System project. Please help me create a Node.js script that will:

1. FIRST, create a backup directory called "backup_cleanup_[timestamp]" and copy files there before deletion

2. Identify and list (but DON'T delete yet) the following categories of files:
   
   TEST FILES TO REVIEW:
   - fix-transcription.js
   - test-transcription.js
   - testModules.js
   - migrate.js
   - indextemp.js
   - Any files starting with "test-" or ending with "-test.js"
   - Any files ending with ".test.js" or ".spec.js"
   
   TEMPORARY/DEBUG FILES:
   - webhookDebugger.js (check if actively used)
   - Files in /uploads/audio/ older than 24 hours
   - Any .tmp or .temp files
   
   DEVELOPMENT ARTIFACTS in /attached_assets/:
   - Screenshot_*.png/jpeg files
   - Pasted-*.txt files
   - IMG_*.PNG that aren't the logo
   - Old CSV exports (transcription_queue_rows_*.csv)
   
   LOG FILES:
   - Any .log files
   - Any npm-debug.log or yarn-error.log
   
   BUILD ARTIFACTS:
   - node_modules/.cache (if exists)
   - Any .DS_Store files (Mac)
   - Thumbs.db files (Windows)

3. The script should:
   - Show me a list of files it found in each category
   - Show file sizes and last modified dates
   - Ask for confirmation before deleting each category
   - Keep a deletion log in cleanup_log_[timestamp].txt
   - PRESERVE these critical files:
     * webhookDebugger.js (if referenced in index.js)
     * All files in /lib/ except test files
     * All production HTML files in /public/
     * Logo files (Car and scales logo_*.png)
     * Template.pdf
     * All .js files in /services/
     * constants.js
     * index.js (main server)
     * package.json and package-lock.json
     * .env file

4. Also check for:
   - Duplicate files (like multiple versions of same file)
   - Empty directories that can be removed
   - Unusually large files that might be debug dumps

5. After cleanup, run these checks:
   - Verify index.js still runs without errors
   - Check that all require() statements in index.js can still resolve
   - List any broken imports/requires found

Create the script as 'cleanup.js' and make it interactive so I can choose what to delete. Include colored console output using chalk or colors package for better readability. Add a --dry-run flag to just show what would be deleted without actually deleting.

IMPORTANT: Be extra careful with:
- Any files referenced in index.js imports
- The /lib/ directory (these are core modules)
- The /public/ directory (production frontend)
- Database migration files that might be needed