Replace all console.log statements in the webhook test endpoint with proper Logger calls.

In the /webhook/signup-simple endpoint (starting around line 1831), replace:

1. Find: console.log('=======================================');
   Replace: Logger.info('=======================================');

2. Find: console.log('SIMPLE WEBHOOK TEST - RECEIVED REQUEST');
   Replace: Logger.info('SIMPLE WEBHOOK TEST - RECEIVED REQUEST');

3. Find: console.log('Headers:', req.headers);
   Replace: Logger.debug('Headers:', req.headers);

4. Find: console.log('Body:', JSON.stringify(req.body, null, 2));
   Replace: Logger.debug('Body:', JSON.stringify(req.body, null, 2));

5. Find: console.log('❌ Authentication failed');
   Replace: Logger.warn('Authentication failed');

6. Find: console.log('✅ Authentication successful');
   Replace: Logger.success('Authentication successful');

7. Find: console.log('Extracted:', { email, name, phone });
   Replace: Logger.debug('Extracted:', { email, name, phone });

8. Find: console.log('Testing Supabase connection...');
   Replace: Logger.info('Testing Supabase connection...');

9. Find: console.log('Inserting data:', insertData);
   Replace: Logger.debug('Inserting data:', insertData);

10. Find: console.log('⚠️ Supabase error:', error.message);
    Replace: Logger.error('Supabase error:', error.message);

11. Find: console.log('Error details:', error);
    Replace: Logger.debug('Error details:', error);

12. Find: console.log('✅ Data saved:', data);
    Replace: Logger.success('Data saved:', data);

13. Find: console.log('Database error:', dbError);
    Replace: Logger.error('Database error:', dbError);

14. Find: console.log('⚠️ Supabase not configured - returning echo');
    Replace: Logger.warn('Supabase not configured - returning echo');

15. Find: console.error('❌ Webhook error:', error);
    Replace: Logger.error('Webhook error:', error);

16. Find: console.log('✅ Simple webhook test endpoint registered at /webhook/signup-simple');
    Replace: Logger.success('Simple webhook test endpoint registered at /webhook/signup-simple');

Also find around line 2433:
console.log(`Processing audio for user ${create_user_id}`);
Replace with:
Logger.debug(`Processing audio for user ${create_user_id}`);